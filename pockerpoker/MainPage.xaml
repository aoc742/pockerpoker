<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="pockerpoker.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Label Grid.Column="1" Text="Color Poker" Style="{StaticResource Headline}" />
                <HorizontalStackLayout Grid.Column="2">
                    <Label Text="Score: " Style="{StaticResource Headline}" />
                    <Label Text="{Binding Score}" Style="{StaticResource Headline}" />
                </HorizontalStackLayout>
            </Grid>

            <Grid HeightRequest="185">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" BackgroundColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource Gray100}}">
                    <Label Text="{Binding Card1}" TextColor="Black" HorizontalOptions="Center" VerticalOptions="Center"/>
                </Border>
                <Border Grid.Column="1" BackgroundColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource Gray100}}">
                    <Label Text="{Binding Card2}" TextColor="Black" HorizontalOptions="Center" VerticalOptions="Center"/>
                </Border>
                <Border Grid.Column="2" BackgroundColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource Gray100}}">
                    <Label Text="{Binding Card3}" TextColor="Black" HorizontalOptions="Center" VerticalOptions="Center"/>
                </Border>
                <Border Grid.Column="3" BackgroundColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource Gray100}}">
                    <Label Text="{Binding Card4}" TextColor="Black" HorizontalOptions="Center" VerticalOptions="Center"/>
                </Border>
                <Border Grid.Column="4" BackgroundColor="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource Gray100}}">
                    <Label Text="{Binding Card5}" TextColor="Black" HorizontalOptions="Center" VerticalOptions="Center"/>
                </Border>
            </Grid>
            
                
            <Label
                Text="{Binding Headline}"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="{Binding CounterBtn}" 
                Command="{Binding OnCounterClickedCommand}"
                SemanticProperties.Hint="Counts the number of times you click"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
